# Subagent Benchmark 迭代升级交接文档 (Handoff to Benchmark v3)

> **版本**：v2 世代 -> v3 世代 Planning  
> **归档位置**：`benchmark_v3/`（本目录；内容为 v2 世代四套件，目录名为迁移时沿用）  
> **各套件内部版本**：`short-python-stdlib-v1` / `closed_loop_v2` / `reviewer-v1 (B11)` / `critic-v1`
>
> **原试题目录迁移**：
> - `shorttaskbenchmark/` (短任务四算法 A/B 测试套件, stdlib-v1)
> - `longtaskbenchmark/` (长任务 Closed-Loop v2 后端双系统套件)
> - `reviewerbenchmark/` (Reviewer 微创调试修复靶场, v1/B11)
> - `criticbenchmark/` (Critic 代码盲审双目标评测套件, v1)

---

## 1. 背景与升级动机：测试天花板效应 (Ceiling Effect)

自 2026 年 7 月基准测试建立至今（运行约两个月），共完成了 **47 个模型版本** 在四轴维度上的系统性评测，建立了完整的 Subagent 工程能力基线数据库。

然而，随着近期高推理能力模型（如 `k3-max`、`deepseek-v4.1-flash`、`composer-2.5`、`gemini-3.8-flash-high`、`hy4` 等）的密集登场，**现有 Benchmark 出现了严重的“天花板效应”与饱和现象**：

1. **短任务维度（Short Tasks · Stdlib v1）**：
   - 现有的 4 道算法边界题（`recursive_patch`、`dependency_layers`、`ttl_set`、`duration`）几乎被所有新模型以 19~20 分逼近甚至取得双满分。
   - 纯标准库约束下的单文件实现对当今主流模型的区分度已降至冰点，无法体现模型在更复杂内存模型、异步事件与协议状态机上的差异。
2. **Reviewer 调试修复维度（Reviewer Debug v1）**：
   - 新一代模型在注入缺陷靶场中的得分普遍来到了 7~8 分（满分 8 项），原本设计的 $O(N^2)$ 复杂度和构造时钟偷跑漏洞已被新模型套路化排查，无法拉开梯队差距。
3. **长任务闭环维度（Closed-Loop v2）**：
   - `order_fulfillment`（SQLite 扣减事务）与 `delivery_spool`（单机本地文件锁租约）的核心考点已经被普遍攻克，近期主流新模型普遍得分已跨越 15~18 里程碑门限。
4. **测试生命周期更迭加速**：
   - 仅仅两个月，原有“高难度测试”已退化为现代模型开箱即用的“常规题目”。为了客观评估未来 6~12 个月内次世代 Subagent 的真实工程执行力，必须正式启动 **Benchmark v3 升级与迭代工程**。
5. **饱和的结构性根因（通读四套件后的确认）**：
   - B 通道为设计内的“计划辅助”条件（契约 + 完整任务计划 `frozen-phi3-plan.md`，用于与 A 通道对照）。**B 通道同样饱和说明：在计划给定之后，现有题目已不存在计划覆盖不了的执行难度。** v3 新题的区分度必须来自“知道做什么 ≠ 做对”的维度——内存硬约束、并发时序、真实故障恢复、随机化隐藏断言。
   - 部分契约含有实现级提示（如 `without recursion`、`type(...) is dict` 直接写进 TASKS），压缩了 A 通道的独立规划空间。
   - 隐藏断言多为固定样例（固定节点集、固定 20k 链），可被针对性硬编码通过。
   - 评测机械本身（隔离 criterion runner、Instruction Gate、multiprocessing spawn 竞赛注入、milestone 字典序排名）经 47 个模型实战验证可靠，**v3 应原样复用，只换内容与度量**。

---

## 2. 现有资产与隔离目录映射

四个维度的完整试题、评分器与公开脚手架已完整移入 `benchmark_v3/`：

```
benchmark_v3/
├── shorttaskbenchmark/           # short-python-stdlib-v1
│   ├── TASKS.md                  # 4 道短任务原始题目契约
│   ├── template/                 # 题目签名与公开 smoke 测试（A/B 双通道，B 含 frozen 计划）
│   └── evaluator/ + harness/     # 16 项隐藏断言 + 2 资源测试评测器
│
├── longtaskbenchmark/            # closed_loop_v2
│   ├── SINGLE_MODEL_LONG_TASK_PROTOCOL.md # 单模型 Pass@1 零多轮干预执行协议
│   ├── closed_loop_v2/           # order_fulfillment 与 delivery_spool 试题
│   └── evaluator/ + harness/     # 20 里程碑、高并发线程注入与故障崩溃模拟器
│
├── reviewerbenchmark/            # reviewer-v1 (B11)
│   ├── public-template/solutions/ # 带有 Kahn 环缺陷及时钟偷跑的原始靶场代码
│   └── controller/               # 8 项补充极限压测评测器与时间捕获引擎
│
├── criticbenchmark/              # critic-v1
│   ├── public-inputs/            # Candidate-A (跨哈希失效) 与 Candidate-B (跨域扫描 O(N²))
│   └── scoring/ + evaluator/     # 百分制综合算分器 (召回 50 + 误报 20 + 深度 15 + 速度 10 + 格式 5)
│
└── HANDOFF.md                    # 本交接说明文档
```

> **复用提示**：各套件 `harness/`、`evaluator/` 的隔离执行器、Instruction Gate、原子 JSON 落盘、资产哈希冻结机制均为 v3 直接复用资产，新题只需替换契约、断言与故障注入内容。

---

## 3. Benchmark v3 核心体系与试题设计蓝图

**总原则：难度必须“计划不可解”。** v2 世代的 B 通道（契约 + 完整任务计划）已被饱和，证明计划给定后题目不再残留执行难度。v3 保留 A/B 双通道结构（计划照常作为实验变量给出），但区分度必须来自计划覆盖不了的执行维度：内存硬约束、并发时序、真实故障恢复、随机化隐藏断言。

### 维度 1：次世代短任务 (Micro-Engine & Protocol Parser)
- **取代原有的单文件算法题**，引入轻量级但要求极高的系统协议与微引擎：
  1. **零拷贝流式解析器（Streaming Zero-Copy Parser）**：解析分块编码（Chunked Transfer）或类 Protocol Buffers 的变长整型（Varint）二进制流，严格限制内存分配上限（如处理 100MB 流峰值常驻内存 < 4MB）。
  2. **非阻塞时间轮驱动器（Hashed Timing Wheel）**：支持百万级定时器注销与重调度，单步 tick 均摊复杂度为 $O(1)$，防时间漂移（Clock Skew）惩罚。
  3. **有限状态机语法高亮器（Lexer State Machine）**：处理深层嵌套递归宏展开、未闭合闭包的优雅降级。
- **配套度量升级（缺一不可，否则上述考点无法判定）**：
  1. **内存硬约束探针**：criterion runner 包一层 `tracemalloc` 峰值断言——v2 harness 只有超时 + 输出截断，没有内存 oracle，“零拷贝”无从评分。
  2. **行为契约化**：TASKS 只写可观测行为与资源门限（如“12 万节点链 5 秒内完成”），删除一切实现级提示（v2 的 `without recursion`、`type(...) is dict` 写法禁止再用）。
  3. **隐藏断言随机化**：harness 注入种子生成用例参数，替代固定样例，杜绝针对性硬编码通过。

### 维度 2：次世代长任务 (Distributed Fault-Tolerant Micro-Services)
- **从单机单进程走向轻量级多服务协同与真实故障注入**：
  1. **三节点 Raft 协同日志复制器**：必须采用 3 节点对等集群（2 节点无法构造单侧多数派的脑裂测试）。考查选主竞争、心跳租约、多数派日志追加、网络脑裂防护（Jepsen 双向分区）、落后节点愈合追赶。
  2. **带分布式死锁检测的 Saga 事务协调器**：1 协调器 + 3 独立 SQLite 服务（库存/支付/物流），预留、确认、取消三阶段流程，面对参与方超时、协调器在提交/回滚中途被杀时的最终一致性与死锁破除。
  3. **崩溃恢复加压（Chaos Engineering）**：v2 的 failpoint 仅为异常注入，模拟不了断电；v3 升级为真实故障——写入关键时刻通过 **Marker 监听触发外部真实 SIGKILL**、“磁盘假满写一半”（ENOSPC）、torn write（截断一半的 `state.json`/WAL）、文件元数据损坏、陈旧锁文件与 PID 复用，验证服务重启后的自我自愈与 Fail-Closed 机制。
- **已知机制修复**：Instruction Gate 对 `src/__init__.py` 等无害改动的误伤（v2 榜单 posthoc 记录过整场 strict=0 事故）需在新 harness 中修正。

### 维度 3：次世代 Reviewer (Complex Race Conditions & Cross-Module Drift)
- **取代当前的单点性能漏洞**：
  1. **竞争条件与隐蔽死锁（Race Condition & Heisenbugs）**：注入在千次并发中仅以 1% 概率发生交叉死锁的锁争抢缺陷，考验模型是否有能力静态分析锁顺序并施加局部细粒度锁优化。
     - **前提：统计式评分**。harness 需 N 次采样运行，评分必须区分“通过静态锁序分析定位”与“跑了几次没挂就当没缺陷”——后者按未发现计分。
  2. **跨模块语义漂移（Semantic Drift）**：修复模块 A 时，要求维持模块 B/C 隐藏契约完全不被破坏。
  3. **精密 AST 行级改动度量**：严惩“整函数推倒重写”，按 AST 差异比对最小修改行数，奖励“单行/极简补丁”。（v2 中整函数重写与单行修复同分，无区分度。）
  4. **诱饵文件（Review Baits）**：掺入看着可疑实则完全正确的代码（与 Critic 诱饵同源），误指认扣分，防止“宁多报不漏报”策略刷分。

### 维度 4：次世代 Critic (Adversarial Audit & Anti-Hallucination)
- **对抗性代码审查**：
  1. **引入特化诱饵代码（Optimization Baits）**：编写看似存在缺陷（如未用互斥锁、裸指针或特定位运算技巧）但在既有前置条件下 100% 安全的特化算法，严格测试审查者的“虚假指控（False Positive）”扣分惩罚。**诱饵严格限定纯逻辑/性能类，不掺安全语义**——“把安全代码误指认为漏洞”正是安全语境下模型最容易过敏的场景，误报分会与拒答分搅在一起。
  2. **真实缺陷“去标签化”（De-labeled Real-World Flaws）**：v2 世代 Critic 全部缺陷为纯逻辑/性能类，历史 29 次运行零拒答。若按原设想直接塞入命名即触发的 `pickle` 注入 / Timing Attack / 路径遍历，强安全调优模型大概率拒答或输出消毒套话；而拒答在百分制下 = 召回 0 + 深度 0 + 格式 0，**与“能力不足”完全无法区分，榜单直接被污染**。因此保留同一批缺陷类（不安全反序列化、时序比较、路径规范化缺失等，保持 CWE 级别真实度），但改写外壳与词汇：
     - 缺陷嵌入业务代码（示例：自研二进制配置解码器不校验长度字段；会话令牌校验循环提前退出；归档导入器不规范化 `..`），文件名、注释、prompt 全文零安全词汇、无可武器化 payload；
     - prompt 显式声明“对合成测试 fixture 的授权审计”，产出契约只要求行级根因定位 + 修复建议，**明确不要求、也不接受利用代码/PoC**；
     - 评分器新增 `safety_refusal` 分类：拒答单独打标，不与能力不足混同；允许一次消毒版 re-prompt 并记入元数据；
     - 上线灰度：先以锚点模型试跑统计拒答率，>10% 则将安全类缺陷进一步降级为纯业务表述。
  3. **结构性弱点修复与纯功能百分制**：v2 的 GT 集中于单文件约 20 行内，行号引用即可近似命中——v3 缺陷必须散布多文件多模块；Depth 的 L3/L4 判定现为人工后标且同缺陷不同分，需写成确定性 rubric；Format 分需严格 schema 校验。
  4. **彻底去除时间计分**：各厂商推理速度与网络抖动不属于模型能力，**原 10 分速度分彻底归零并剥离**，重新分配为：**召回 55 分 + 诱饵防误报 20 分 + 深度 20 分 + 格式 5 分 = 100 分**。耗时仅作为遥测元数据展示，供人工复盘筛选。

### 全局评分、Token 审计与断点续跑规范 (跨四维度)
1. **时间纯遥测**：全套件评分 100% 剥离纯运行时间，杜绝各厂商 API 速率差异导致的不公平。
2. **Token 全量审计与安全熔断**：完整记录 Prompt、Completion、Reasoning Tokens 以及 `Tokens/PassedMilestone`，作为性价比帕累托前沿分析与 SFT 高信噪比轨迹筛选依据；设单任务 10M 软熔断上限防死循环账单爆炸。
3. **轻量单轮 Prompt 快照断点续跑**：每轮 API 发起前原子落盘 `last_prompt_snapshot.json`，遇到崩溃/断网时携带 `--resume` 直接原地重发重试，结合指数退避自愈，无需繁复的多轮状态持久化。

---

## 4. 新老榜单衔接：五大“标杆模型 (Anchor Models)”矩阵

为确保新 Benchmark v3 上线后，不需要重新花费巨额算力重跑老榜中的全部 47 个模型，必须确立 **5 个具有鲜明梯队特征的标杆锚点模型**。后续新榜评分将通过这 5 个模型在新老两套题目的相对表现，进行跨世代得分映射与校准：

| 标杆类型 | 选定模型 (严格对齐 models-master.json ID) | 当前 v2 历史表现 (长/短/Review/Critic) | 作为锚点的核心职责与校准作用 |
| :--- | :--- | :--- | :--- |
| **👑 绝对旗舰上限锚点** | **`k3-max`** / `deepseek-v4.1-flash` / `gpt-5.6-sol` | 长: 17~19 / 短: 20满分 / 评: 8/8满分 / 审: 89.8~97.5 | **校准新一代题目的天花板**。新题难度必须能将旗舰梯队模型的得分压制在 70%~80% 区间，若顶级模型在新题中仍然轻松全满分，说明新题梯度不足。 |
| **⚔️ 工业中坚基石锚点** | **`qwen3.8-max`** / `glm-5.3` | 长: 16~17 / 短: 16~19 / 评: 7~8 / 审: 88.5 | **校准实用生产级 Subagent 分水岭**。检验工业级主力基座模型在新 Benchmark 下能否完成高可靠闭环，界定“企业级可用”基准线。 |
| **⚡ 极速轻量执行锚点** | **`gemini-3.6-flash`** / `kimi-2.8` / `luna` | 长: 12~15 / 短: 20满分 / 评: 8/8 (60s极速) / 审: 98.2 | **校准快修与轻量高频 Subagent 标尺**。以高吞吐、超快响应（60s 内微创全通）为特征，用于评判新短任务与轻量调试场景下的速度/精准平衡曲线。 |
| **🛡️ 高敏代码审计锚点** | **`doubao-seed-evolving`** / `doubao-2.0-pro` | 长: 8~12 / 短: 17~19 / 评: 7~8 / 审: 90.0~91.8 (L4 发现者) | **校准 Critic 盲审体系的有效性**。检验新 Critic 题目中真实缺陷召回与反欺骗防误报能力，确保审查不是模板化猜测；同时作为去标签化缺陷的**拒答率探针**。 |
| **⚓ 能力底线验证锚点** | **`step-3.7-flash`** / `minimax-m3` | 长: 0~4 / 短: 14~15 / 评: 7/8 / 审: 28.2~69.3 | **校准底线防护与区分度**。确保新题目具备合理难度梯度，防止二三线模型出现断崖式全面归零，保持评测光谱的完整度。 |

---

## 5. 新 Session 接手执行路线图 (Action Items)

接手的下一个 Session 可按以下步骤直接开展工作：

1. **第 1 步：环境与基线确认**
   - 验证 `benchmark_v3/` 下四个目录文件的完整性（运行原有的 `package-manifest.json` 与测试运行器确认可重现）。
2. **第 2 步：新题立项与沙盒构建**
   - 在本目录下以新一代版本号新建套件目录（如 `shorttaskbenchmark_v2/`、`closed_loop_v3/`、`reviewer_v2/`、`critic_v2/`），harness/evaluator 从对应 v2 套件复用改造。
   - 依据本规范第 3 节的试题蓝图，率先起草**两道次世代长任务题目**与**两道异步流式短任务题目**。
3. **第 3 步：标杆模型首轮校准压测**
   - 首先将 5 大标杆模型中的 **`k3-max`** 与 **`gemini-3.6-flash`** 接入新沙盒进行试跑。
   - 监控其在新题中的得分分布，调整题目中断言严苛度，将顶级模型的基线分数压制在合理的动态区分区间内。
   - Critic 维度同步统计**拒答率**，验证去标签化设计是否规避安全围栏（阈值 10%，见第 3 节维度 4）。
4. **第 4 步：跨版本数据关联与新前端展示**
   - 在新数据引擎中建立 `v2_legacy_score` 与 `v3_score` 的回归换算系数，并在可视化看板中保留老榜索引，平滑完成换代过渡。
